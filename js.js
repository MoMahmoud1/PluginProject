(function ($) {

    $.fn.controlImage = function(){

        $.fn.slider = function (Options) {
            let settings = $.extend(
              {
                fadeOut: 1000,
                fadeIn: 1000,
                time:3000
              },
              Options
            );
        


    }

})(jQuery);